import Foundation


func simpleArraySum(ar: [Int]) -> Int {
    // Write your code here
    let sum = ar.reduce(0) { result, element in
        result + element
    }
    return sum
}

func compareTriplets(a: [Int], b: [Int]) -> [Int] {
    // already done
}

func aVeryBigSum(ar: [Int]) -> Int64 {
    // already done
}

func extraLongFactorials(n: Int) -> Void {
    // already done
}
